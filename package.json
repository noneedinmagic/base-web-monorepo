{
  "name": "@noneedinmagic/base-web-monorepo",
  "version": "0.0.0",
  "description": "Basic web monorepo for FE+BE by Tron",
  "main": "index.js",
  "engines": {
    "node": ">=16.3",
    "npm": ">=8"
  },
  "workspaces": [
    "packages/shared",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -ws --if-present",
    "test": "npm run test -ws --if-present"
  },
  "author": "Tron <noneedinmagic@gmail.com>",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "rimraf": "3.0.2",
    "typescript": "4.8.4"
  }
}
