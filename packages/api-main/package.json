{
  "main": "dist/main.js",
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "tsc": "tsc"
  },
  "devDependencies": {
    "@types/node": "16.11.62"
  }
}
